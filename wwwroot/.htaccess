RewriteEngine On

# Internally rewrite /mcp (no trailing slash) to mcp/index.php.
# Apache's default 301 redirect converts POST→GET, dropping body and Authorization.
# An internal rewrite serves the file directly — no redirect, no method change.
RewriteRule ^mcp$ mcp/index.php [L,QSA]
